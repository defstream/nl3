/**
 * returns the first item in an array
 * @param  {Array} array The array to return the first item from.
 * @return {*}  The value of the first item in the array or undefined.
 */
module.exports = function first(array) {
  return array[0];
}
